using TakAI.Models;

namespace TakAI.Services
{
    public class TurnService
    {
        public Board AddPiece(Board board, int x, int y, string color)
        {
            return board;
        }

        public Board MovePiece(Board board, Stone stone, int x, int y)
        {
            return board;
        }
    }
}